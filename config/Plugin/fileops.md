- 写入文件: <add>文件名\n文件内容(可多行)</add>，默认UTF-8编码
- 修改文件: <mod>(文件路径)|(起始行,结束行)
修改内容</mod>
- 恢复备份: <res>文件名</res>，将文件恢复为最近一次操作前的状态（自动从 .bak 恢复）

所有文件操作会自动在同目录创建 .bak 备份，可通过 <res> 标签或备份面板恢复。
如需定位出错行号，优先使用PowerShell命令搜索或读取文件，优先精确修改文件。